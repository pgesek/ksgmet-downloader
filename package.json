{
  "name": "ksgmet-downloader",
  "version": "1.0.0-SNAPSHOT",
  "description": "Downloader function for KSG-Met",
  "main": "src/index.js",
  "scripts": {
    "build-aws-resource": "mkdir -p dist && pack-zip && mv *.zip dist",
    "build-aws-resource-win": "if not exist dist mkdir dist && pack-zip && move *.zip dist"
  },
  "author": "Paweł Gesek",
  "license": "MIT",
  "devDependencies": {
    "pack-zip": "^0.2.2"
  }
}
